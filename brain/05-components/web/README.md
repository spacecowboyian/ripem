# Web components

This directory contains documentation for web-specific components.

Each component subdirectory follows this structure:

```
/[component-name]
  overview.md        — What the component does and when to use it
  implementation.md  — React (or framework) implementation details
  examples.md        — Code examples and screenshots
```

> *(Add component subdirectories here as web components are documented.)*
