# iOS components

This directory contains documentation for iOS-specific components.

Each component subdirectory follows this structure:

```
/[component-name]
  overview.md        — What the component does and when to use it
  implementation.md  — SwiftUI / UIKit implementation details
  examples.md        — Code examples and screenshots
```

> *(Add component subdirectories here as iOS components are documented.)*
