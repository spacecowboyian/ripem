# Environments

This document describes the environments in which ripem runs.

## Environments

| Name | Purpose | iOS scheme | Web URL |
|------|---------|------------|---------|
| Development | Local developer machines | `Development` | `localhost` |
| Staging | Pre-production testing | `Staging` | *(to be defined)* |
| Production | Live users | `Production` | *(to be defined)* |

## Environment variables

> *(List environment variables used across platforms. Never store values here — document the variable name and purpose only.)*

| Variable | Purpose | Platforms |
|----------|---------|-----------|
| *(to be defined)* | | |

## Secrets management

> *(Describe how secrets are stored and accessed — e.g., environment-specific `.env` files (never committed), a secrets manager, etc.)*
